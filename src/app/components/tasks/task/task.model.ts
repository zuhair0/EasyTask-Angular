export interface taskObject {
  id: string;
  userId: string;
  title: string;
  summary: string;
  dueDate: string;
}

export interface newTaskModel {
    title: string;
    summary: string;
    dueDate: string;
  }
  